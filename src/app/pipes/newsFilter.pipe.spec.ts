/* tslint:disable:no-unused-variable */

import { NewsFilterPipe } from './newsFilter.pipe';

describe('Pipe: NewsFiltere', () => {
  it('create an instance', () => {
    const pipe = new NewsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
